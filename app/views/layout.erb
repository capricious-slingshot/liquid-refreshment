<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8">
    <title>Liquid Refreshment</title>
    <meta name="description" content="Beer Leaderboard for Cloudburst Brewing">
    <meta name="author" content="capricious-slingshot">
    <link rel="stylesheet" type="text/css" href="/stylesheets/reset-stylesheet.css" media="screen"/>
    <link rel="stylesheet" type="text/css" href="/stylesheets/stylesheet.css" media="screen"/>
  </head>

  <body>
    <div id="mainContent">
      <nav>
        <ul>
          <% if !logged_in? %>
            <li><a href="/login">Log In</a></li>
            <li><a href="/signup">Signup</a></li>
          <% else %>
            <li><a href="/settings" id="settings-icon"><img src="/images/settings-icon.png" alt="User Settings"></a></li>
            <li><a href="/leaderboard">Leaderboard</a></li>
            <li><a href="/">My Ratings</a></li>
          <% end %>
        </ul>
      </nav>
      <h1>Cloudburst Brewing</h1>
      <% if flash[:success] %>
        <p class="flash success"><%= flash[:success] %></p>
      <% elsif flash[:error] %>
        <% flash[:error].each do |message| %>
          <p class="flash error"><%= message %></p>
        <% end %>
      <% end %>
      <%= yield %>
    </div>

  </body>

</html>
